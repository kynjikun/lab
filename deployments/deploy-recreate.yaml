apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: test-deployment
  name: test-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: test-deployment
  strategy: 
    type: Recreate
  template:
    metadata:
      labels:
        app: test-deployment
    spec:
      containers:
        - image: httpd:alpine3.18
          name: httpd
          resources: {}
status: {}
